#include<stdio.h>
int main()
{
	int x[10],n,i,sum=0;
	printf(" Enter the number of variables you want :");
	scanf("%d",&n);
	printf("Enter elements :");
	for(i=0;i<n;i++)
	{
		scanf("%d",&x[i]);
	}
	for(i=0;i<n;i++)
	{
		sum=sum+x[i];
	}
	printf("\n sum = %d",sum);
	return 1;
}
